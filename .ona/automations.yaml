tasks:
  setupCredentials:
    name: Setup AI tool credentials
    description: Configure Claude Code, Continue.dev, Roo Code, and GitHub Copilot CLI using environment secrets.
    command: |
      bash .devcontainer/scripts/setup-credentials.sh 2>&1 | tee /workspaces/setup.log
    triggeredBy:
      - postDevcontainerStart
